# Optional extras. Currently empty: yt-dlp moved into requirements.txt because
# video-site links are a wanted feature, and it fits the serverless budget.
# Kept as the place for future optional dependencies.
